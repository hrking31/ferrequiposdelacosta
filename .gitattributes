# Fuerza LF en archivos de texto, sin importar la config de git de cada máquina
# (evita que en Windows con core.autocrlf=true los .js queden en CRLF y
# rompan el lint de eslint-config-google en functions/, que exige LF).
* text=auto eol=lf

*.js text eol=lf
*.jsx text eol=lf
*.json text eol=lf
